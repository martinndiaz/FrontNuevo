# Variables de entorno para el frontend (referencia manual)
# Copia este archivo como .env y sincroniza el valor en env.js si no cuentas con un bundler.
API_BASE_URL=https://jacelyn-indictable-lashaunda.ngrok-free.dev